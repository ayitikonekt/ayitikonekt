{
  "name": "ayitikonekt-functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "check": "node --check index.js",
    "test:rules": "firebase emulators:exec --config ../firebase.json --only firestore --project demo-ayitikonekt-security \"node --test test/firestore.rules.test.js\"",
    "test:security": "node --test test/security.test.js",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
